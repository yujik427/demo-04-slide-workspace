# demo-04 スライド量産 4ペイン作業場アプリ

- オーナー: Yuji Kubo
- CEO（意思決定者）: Jobs
- 作成日: 2026-05-11
- ステータス: **v0.2 方針更新 / 裏生成型スライド制作ワークスペースへ再定義**
- 原則: `knowledge/tool-development/shared-principles.md`
- 接続元: `projects/crowdworks/deliverables/2026-05-09_cc-slide-generator-task/cc-slide-generator/`

---

## 0. 結論

CLIが完成した後の次の一手は、CLIの結果を手で微修正するだけの画面ではない。

次に作るのは、完成したCLIを生成エンジンとして取り込み、**同じ粒度・同じ品質でスライドを何セットも生成できるWebアプリ型の作業場**である。

```text
A: cc-slide-generator
YAML / Markdown -> AI生成 -> JSON -> HTML -> PNG

B: slide-workspace
案件・目的 / 原稿 / 抽出パーツ / 生成スライド確認・編集・再生成を4ペインで扱うWebアプリ
```

BはAを置き換えない。AのCLIで確立した生成パイプラインを、非エンジニアでも扱える作業場に載せる。

v0.1で作った「スライド一覧 / コピー編集 / 小プレビュー / 大プレビュー」は破棄しない。ただし、それはB全体の主画面ではなく、v0.2では4ペイン目の「生成スライド確認・編集・再生成」の部品として扱う。

---

## 0.1 v0.2で確定した方向転換

このアプリの主役は、完成後のコピーを人間が1枚ずつ直す画面ではない。

本来の流れは、裏側で以下を進め、表側ではその結果を確認・選択・部分再生成することである。

```text
相談・企画
-> 原稿化
-> スライドに入れるパーツ抽出
-> スライドルールに沿った短文化・キャッチ化
-> スライドJSON化
-> HTML / PNG化
-> 必要に応じてイラスト生成・差し込み
```

表側の4ペインは、この裏側の制作工程を見失わずに扱うための作業場にする。

```text
[Pane 1] 案件・目的
[Pane 2] 原稿
[Pane 3] 抽出パーツ
[Pane 4] 生成スライド確認・編集・再生成
```

コピー編集はPane 3の主役ではない。スライド生成後に必要な最小調整として、Pane 4の中に置く。

---

## 1. 表現ルール

この設計書や外部向け資料では、外部由来の固有名詞や出典が透ける言い回しを出さない。

良いものは取り入れるが、表現は必ず自分たちの実務上の判断として書く。

- NG: 外部で学んだ型に従う
- OK: 制作工程を分解し、頻繁に行き来する情報を1画面にまとめる
- NG: 他所の作業場アプリを参考にした
- OK: 入力・構成・コピー・スライド確認を同時に扱える作業場にする
- NG: 4ペインの型をそのまま使う
- OK: 全体から詳細へ迷わず移動できる4領域の画面にする

---

## 2. 自作判断ゲート

### 既存ツールで十分か

Googleスライド、Canva、Gamma、Notion、Figma、PowerPointなどで、スライドを1枚ずつ作ることはできる。

しかし、Yujiが欲しいのは単なるスライド作成ツールではない。

必要なのは、CLIで確立した生成品質を保ったまま、以下を1つの作業場で回せること。

- 新しい案件・商品情報を入力する
- 原稿を制作元として確認する
- 原稿からスライド用パーツを抽出・短文化する
- パーツをスライド構成へ割り当てる
- スライドJSONを生成・更新する
- 生成済みスライドを確認・部分再生成する
- 大プレビューで本番表示を確認する
- HTML/PNGとして出力する
- 必要に応じてイラストを生成・差し込む
- 複数バリエーションを同じ品質で作る

既存ツールは完成物の編集には強い。一方で、**AI生成パイプラインを使って、同じ品質のスライドを量産する作業場**としては足りない。

### 自作で勝つ1行

> CLIで確立したスライド生成パイプラインを、案件・原稿・抽出パーツ・生成スライド確認まで扱える4ペインWebアプリに載せ替え、同じ粒度・同じ品質のスライドを何セットも作れるようにする。

---

## 3. CLI完成後に扱う工程

ここで分解するのは、CLI自体を作る工程ではない。
対象は、CLIが完成し、生成パイプラインが動くようになった後に、Webアプリとして扱う工程である。

1. 新しい案件・商品情報を入力する
2. 生成したいスライドの目的・相手・トーンを指定する
3. 制作元の原稿を確認する
4. 原稿から抽出されたスライド用パーツを確認する
5. どのパーツをどのスライドへ使うか確認する
6. 生成済みスライドを確認する
7. 必要なスライドだけコピー編集・再生成する
8. 必要に応じてイラスト生成・差し込みを行う
9. 大プレビューで本番サイズに近い見え方を確認する
10. 文字数・見切れ・重複表現の警告を見る
11. バリエーションを複製して別訴求を作る
12. HTML/PNGを書き出す
13. 提出・共有に必要なリンクやメモを整える

この中で特に面倒なのは、**原稿・抽出パーツ・スライド構成・プレビュー・出力が別々のファイルや画面に分かれること**である。

この作業場アプリは、その往復を減らし、CLIの生成力を普段使いできる形にするために作る。

---

## 4. 作る意味

CLIは「生成できる」ことを証明するための道具である。

Webアプリは、その生成力を継続的に使うための作業場である。

CLIだけでも1セットは作れる。しかし、案件ごとに何度も使うには、毎回ファイルを開き、コマンドを打ち、JSONやHTMLを確認する必要がある。

Webアプリ化する意味はここにある。

> CLIでできる生成を、案件入力・原稿確認・パーツ抽出・スライド確認・再生成・出力まで一つの流れで扱えるようにする。

---

## 5. 最小版の定義

最初から完成版を作らない。まずは、CLIをWebアプリの中で扱えることを確認する。

### v0.2 最小版で作るもの

- サンプル案件を1つ表示する
- 原稿サンプルをPane 2に表示する
- 原稿から抽出されたスライド用パーツをPane 3に表示する
- パーツは `訴求 / 根拠 / 数字 / 不安解消 / CTA / イラスト指示` のような制作判断単位で持つ
- パーツから生成されたスライド一覧とプレビューをPane 4に表示する
- Pane 4で、生成後スライドの一部コピーを編集・再生成できる入口を置く
- title / highlight / body の文字数警告を出す
- 大プレビューモードで本番サイズに近い表示を確認できる
- 最低1回、CLI由来のレンダリング処理をWebアプリ側から呼ぶ設計にする

### 最小版で作らないもの

- ログイン
- DB保存
- 複数ユーザー管理
- Google Drive連携
- 提出フォーム連携
- 権限管理
- 本格的なバージョン管理

最初の目的は、「原稿からスライド材料を作り、生成済みスライドとして確認・再生成できる」と確認すること。

---

## 6. 先に検証すること

全部を一気に作らない。怪しいところだけを先に検証する。

1. 原稿サンプルをWebアプリで読み込み、制作元として表示できるか
2. 原稿から抽出済みのスライド用パーツを表示できるか
3. パーツを選ぶと、どのスライドに使われるか確認できるか
4. 生成済みスライドをPane 4で一覧・プレビュー表示できるか
5. 必要なスライドだけコピー編集・再生成できる入口を置けるか
6. 大プレビューで本番サイズに近い確認ができるか
7. CLI側のHTMLレンダリング処理をWebアプリ側に安全に移せるか
8. 将来のHTML/PNG出力とイラスト生成をどこで実行するか判断できるか

この8点が通れば、次にAI再生成、PNG出力、イラスト差し込み、バリエーション管理を足す価値がある。

---

## 7. AとBの責務分離

| 項目 | A: CLI | B: 4ペインWebアプリ |
|---|---|---|
| 役割 | 生成パイプラインのMVP | 生成パイプラインを継続利用する作業場 |
| 評価軸 | アドネス案件課題 / 再現性 | 量産性 / 品質の安定 / 操作しやすさ |
| 技術 | Node.js + Playwright | Next.js + TypeScript + Tailwind CSS + shadcn/ui |
| 入力 | YAML / Markdown | 案件情報、原稿、抽出パーツ、スライド計画、スライドデータ |
| 出力 | HTML / PNG | スライドJSON、HTMLプレビュー、将来PNG出力、イラスト差し込み結果 |
| 触る時期 | 5/15提出まで | A完成後 |

BはAを改造しない。Bは新規アプリとして作り、Aの生成・レンダリング処理を部品として取り込む。

---

## 8. 4ペイン設計（v0.2）

左から右へ、制作元から生成結果へ落とす。

```text
[Pane 1] 案件・目的
    ↓
[Pane 2] 原稿
    ↓
[Pane 3] 抽出パーツ
    ↓
[Pane 4] 生成スライド確認・編集・再生成
```

右端のペインは、本番デザイン確認だけの場所ではない。
役割は「裏で生成されたスライドを確認し、必要な箇所だけ編集・再生成する」ための操作場である。
本番サイズでの余白・可読性・見切れ確認は、別の大プレビューモードで行う。

### Pane 1: 案件・目的

最大の文脈を置く。

- 案件名
- 対象商品
- 目的
- 読者・視聴者
- トーン
- デッキ名
- バリエーション名
- 出力状態

初期MVPではローカルのモックデータでよい。DBは入れない。

### Pane 2: 原稿

制作元となる原稿を置く。

- 原稿本文
- 章・段落
- 重要そうな文
- まだ原稿が未完成であることの状態表示

ここは「スライド化の材料」を見る場所。初期MVPでは原稿サンプルを表示できればよい。原稿そのものの本格編集は後回しにする。

### Pane 3: 抽出パーツ

原稿から裏側で抽出・短文化された素材を置く。

- 訴求
- 根拠
- 数字
- 不安解消
- CTA
- イラスト指示
- どのスライドに使う候補か

ここは「スライドに何を入れるか」を見る場所。コピーを1枚ずつ手で作る場所ではない。

### Pane 4: 生成スライド確認・編集・再生成

生成済みスライドを扱う。

- スライド一覧
- 選択中スライドのプレビュー
- どのパーツがスライドのどこに出たか
- title / subtitle / highlight / body の最小編集
- このスライドだけ再生成
- イラスト生成・差し込みの入口
- 文字数警告
- 見切れリスクの簡易警告

小プレビューで判断するのは「パーツとスライドの対応関係」と「ざっくりした配置」だけ。
文字の読みやすさ、余白の美しさ、CTAの迫力、本番サイズの見切れはここだけで判断しない。
それらは大プレビューモードで確認する。

### 大プレビューモード

4ペイン常時表示とは別に、本番確認用の大プレビューモードを持つ。

```text
[スライド一覧] [大プレビュー] [最小編集・再生成] [警告・出力チェック]
```

大プレビューモードで確認するもの。

- 文字が本番サイズで読めるか
- 余白が足りているか
- 下部や左右が見切れていないか
- 数字やCTAの視認性が十分か
- 出力前チェックに通るか

---

## 9. MVP定義

### 作るもの

Aで確立した `slides/default.json` とレンダリング処理を使いつつ、原稿から抽出パーツ、抽出パーツから生成スライドへつながる4ペインWebアプリプロトタイプ。

### MVPの完成条件

- 4ペインが1画面に並ぶ
- Pane 1で案件・目的が見える
- Pane 2で原稿サンプルが見える
- Pane 3で抽出済みパーツが見える
- Pane 4で生成スライド一覧とプレビューが見える
- Pane 4で選択スライドの最小編集と再生成入口が見える
- 文字数警告が最低限出る
- 大プレビューモードに切り替えられる

### あえて作らないもの

- ログイン
- DB
- 複数ユーザー
- Google Drive連携
- 提出フォーム連携
- Vercel本番運用の細かい権限設計

MVPでは「裏側で作った原稿・抽出パーツ・スライドを、Webアプリ内で扱う表側」を作る。保存・権限・外部連携・本格的なAI再生成は次フェーズ。

---

## 10. 技術スタック

Webアプリとして長く育てる前提で、技術スタックは固定する。

- Next.js（App Router）
- TypeScript
- Tailwind CSS
- shadcn/ui
- Vercel

プロジェクト開始時の最初の指示は以下に固定する。

```text
Next.js（App Router）／TypeScript／Tailwind CSS／shadcn/ui を使って、CLIのスライド生成パイプラインを組み込んだ4ペイン作業場アプリを作ってください。デプロイ先は Vercel です。
```

---

## 11. AからBへ渡すもの

A側でB着手前に整えるもの。

- `build-html.js` の純粋関数化
- JSONスキーマのREADME明記
- サンプル `slides/default.json`
- サンプル `inputs/source.yml`
- サンプル `inputs/script.md`
- 生成済み `output/default/index.html`

BはAの成果物をただ眺めるのではなく、Aの生成・レンダリング処理をWebアプリ側で扱えるようにする。

---

## 11.1 v0.2の状態モデル

v0.2では、`slides[]` を直接編集するだけの状態設計にしない。

```text
sourceDraft
  原稿。Claude Code等で作られた制作元テキスト。

extractedParts
  原稿から抽出・短文化されたスライド素材。
  例: 訴求 / 根拠 / 数字 / 不安解消 / CTA / イラスト指示。

slidePlan
  どのパーツを何枚目のスライドに使うかの設計。

slides
  最終的にレンダリングへ渡すスライドJSON。
```

この分離により、将来「原稿だけ差し替える」「パーツだけ再抽出する」「1枚だけ再生成する」「イラストだけ差し替える」を同じ画面で扱える。

---

## 12. POCで怪しいところ

最初に検証すべき怪しい点は以下。

1. 原稿サンプルをPane 2に読み込めるか
2. 抽出済みパーツをPane 3に表示できるか
3. パーツと生成スライドの対応関係をPane 4で見せられるか
4. Pane 4で生成済みスライドの一覧・小プレビュー・大プレビューが破綻しないか
5. 1枚だけ再生成するUI導線が分かりやすいか
6. highlightやtitleの文字数警告が実用になるか
7. AのHTMLレンダリング関数をBに安全に移植できるか
8. 将来のHTML/PNG出力、イラスト生成、差し込みをどこで実行するのがよいか判断できるか

---

## 13. 次の実作業

### A完成前

- AのCLIを提出可能状態にする
- `submission-doc.md` にBへの発展計画を明記する
- `build-html.js` 純粋関数化は余力があれば実施する

### v0.2 次の実作業

1. `sourceDraft` のサンプルデータを作る
2. `extractedParts` のサンプルデータを作る
3. `slidePlan` のサンプルデータを作る
4. 既存MVPのPane 1〜4を、`案件・目的 / 原稿 / 抽出パーツ / 生成スライド` へ組み替える
5. 既存のコピー編集・小プレビュー・大プレビューはPane 4へ移す
6. Pane 4に「このスライドだけ再生成」ボタンを置く
7. イラスト生成・差し込みは入口だけ置き、実生成は次フェーズに回す
8. `npm run lint` と `npm run build` で確認する
9. Vercelにデプロイする

---

## 14. Jobs判断

このBは、Aの後処理画面ではない。Aで作ったCLIを、継続的に使えるWebアプリへ引き上げるための別プロジェクトである。

CLIは「生成できる」を証明するためのMVP。4ペイン作業場は「同じ粒度・同じ品質で何度も生成できる」を実現するための道具。Aで生成の再現性を証明し、Bで量産性と操作性を作る順序が正しい。
